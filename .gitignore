# Private eval sandbox — never publish (may contain machine paths / local data)
eval-workspace/

__pycache__/

# real eval/corpus data — never commit
eval_real_corpus.json
.real_corpus_manifest.json

# round-specific campaign sweep scripts (one-time artifacts)
scripts/sweep_r*.py

# eval outputs
eval_run_*.json
EVAL_REPORT.md

# mkdocs build output
site/
