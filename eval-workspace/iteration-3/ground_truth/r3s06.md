Verdict: rate-limiter hypothesis FALSIFIED by dose-response — timeouts are flat across 50/100/200 rps (no gradient with rate), but jump ~3x whenever avg_payload_kb=512 vs 4 (strong gradient with payload size). The cause is large payloads, not request rate; throttling to 50rps would hurt customers and fix nothing. Required: compute timeouts grouped by rps (no effect) AND by payload (clear effect), use dose-response language, recommend payload-size mitigation, block the 50rps cap.