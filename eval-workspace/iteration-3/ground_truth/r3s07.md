Verdict: determinism claim FALSIFIED at the replication level that matters. Re-running build.sh in the SAME directory reproduces the same hash (measurement-level replication passes), but the build embeds $PWD ('// workdir: ...') — a clean-room rebuild from a copied/fresh checkout path produces a DIFFERENT hash, so the hash is not attestation-grade. Required: test both replication levels (same-dir rerun AND fresh-path rebuild), locate the $PWD embed in build.sh, verdict = not deterministic across environments, fix = strip workdir (and any env-dependent content). Passing it after only same-dir reruns = fail.