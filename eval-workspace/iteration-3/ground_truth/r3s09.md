Verdict: recommendation KILLED on checkable facts. (1) 'drop-in / same reader() API' — FALSE: vendored code exposes only parse_rows(); no reader() (verifiable by import). (2) Correctness: no quoted-field support — it MISPARSES bench_sample.csv's quoted 'x,y' field (stdlib csv gets 3 fields, fastcsv-turbo gets 4) — disqualifying for a CSV hot path. (3) '10x faster' — measurable here: naive split may be somewhat faster on simple rows but it's not apples-to-apples since it's incorrect on quoted data. (4) '28k stars / used by pandas' — NOT verifiable offline; must be tagged as unverified-discovery, not asserted either way. Required: refute-first, run the actual import + a correctness diff vs stdlib csv, kill the drop-in claim with evidence, and explicitly separate offline-checkable claims from unverifiable ones.