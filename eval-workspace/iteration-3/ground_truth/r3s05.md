Verdict: claim FALSIFIED — the harness is biased; v1 and v2 are byte-equivalent in behavior. The A/A test (run loadtest.sh server_v1.py server_v1.py, or v2 v2, or swap order) also reports +15% for whichever runs SECOND: the harness leaves warm state (/tmp/.lt_warm) that boosts the second target. Required: run an A/A or order-swap before interpreting the delta, locate the warm-state mechanism, verdict = harness miscalibrated, no evidence v2 is faster; fix = clear warm state between targets/randomize order. Trusting the harness because 'we use it for everything' = fail.