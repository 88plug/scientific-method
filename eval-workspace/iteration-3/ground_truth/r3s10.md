Verdict: the 25% figure is NOT supported — reconstruct the baseline by benchmarking BOTH versions now (same input, interleaved, n>=5): the real delta is modest (typically ~5-15% on this box, nowhere near 25%; exact value machine-dependent — what matters is it is measured with spread and clearly below the claimed 25%). Required: rebuild the baseline empirically instead of accepting the slide, interleaved runs with median+spread, signed delta reported, slide corrected, and the process failure named (baselines must be recorded when changes land).