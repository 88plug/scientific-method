# Invention brief (full pipeline)
alerts.csv is a 12-minute alert flood (~460 alerts). Invent a grouping
algorithm that compresses it into <=8 incident groups such that: the 3 real
root-cause clusters each map to exactly one group (no real cluster split
across groups, no two reals merged), and transient noise doesn't form more
than 5 extra groups. truth.csv has labels — SCORING ONLY, not fitting.
Claim grammar + chart + LIVE provenance search + honest rung + disclosure.
