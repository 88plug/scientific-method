# Invention brief (full pipeline)
Design a streaming structure that estimates p50/p95/p99 of stream.txt (gen.py)
within ±2% relative error using <=64KB of state (measure/account it),
single pass. Compare against exact sorted truth. Claim grammar + chart +
LIVE provenance search + honest rung + disclosure. Known-art families exist —
find and cite them.
