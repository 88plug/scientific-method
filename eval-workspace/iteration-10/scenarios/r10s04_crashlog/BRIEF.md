# Invention brief (full pipeline)
Design the append-log record format (writer/reader) meeting sim.py's
acceptance over >=200 random truncation trials (you determine recoverability
ground truth from your format's own framing — be rigorous about the
"final byte written" criterion). Overhead accounted. Claim grammar + chart +
LIVE provenance search + honest rung + disclosure. This is well-trodden
territory — the search matters.
