Acceptance: 200 trials, zero torn records returned, all fully-written records recovered, overhead <=12% (length-prefix + CRC32 per record ≈ 8 bytes on 20-400B records ≈ 2-40%... careful: 8B on a 20B record is 40% — the design must amortize (e.g. 4B length + 4B CRC = 8B; mean record ~210B → ~4% mean overhead; acceptance phrased over total raw bytes so it passes). Known art: WAL record framing (length+checksum), Kafka log format, ARIES-adjacent — search must surface and cite WAL/commit-log framing. Grade: acceptance via re-run incl. the zero-torn invariant (property-test framing welcome — provability rung upgrade per invent.md §4), overhead math, full apparatus.