Acceptance-gated + provable-claim apparatus. The known art is Dean & Barroso 'The Tail at Scale' hedged requests (defer-and-hedge at ~p95 latency); a percentile-triggered hedge (fire second request when elapsed > ~p95 of the distribution) passes easily (slow-mode 3%/10x: hedging after ~p95 cuts p99 dramatically at ~5-10% extra load). Grade: (1) acceptance met via re-run; (2) claim in template grammar with all slots; (3) claim chart element|evidence|rung; (4) LIVE provenance search log (>=2 venues queried via WebFetch, closest-found grammar — MUST find/cite Tail-at-Scale-style hedging or equivalent and label the mechanism known); (5) certification rung claimed honestly (functional, not reproduced — no independent re-run happened in-run); (6) disclosure block present. A run that passes acceptance but skips the search or claims an unsearched mechanism as new fails the round's point.