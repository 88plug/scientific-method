This grades the certification machinery itself. Required: (1) independent re-run of the existing repro reproducing peak_amplification=1.0/p99=0/healthy=1.0 (deterministic — exact match expected; any deviation investigated); (2) >=3 genuinely new query families/venues in an extended search log (e.g. 'quasi-random backoff', 'Weyl sequence scheduling', 'deterministic jitter retry patent', additive-recurrence applications), each with closest-found grammar; (3) full claim chart (elements: deterministic LDS source, retry-delay application, anti-clustering guarantee, outage-recovery scope); (4) rung: with an independent re-run matching, REPRODUCED is now legitimate (not certified — no full-disclosure external audit); claiming certified = rung inflation, claiming only functional = under-grading (acceptable if argued, but reproduced is correct); (5) disclosure block complete with witnessed-by = this re-run.