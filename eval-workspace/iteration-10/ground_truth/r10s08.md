Acceptance: value_in_slo >= 1.55x baseline's AND p99_wait <= 2s, re-run verified, no future info. Mechanism family: value-based admission with an adaptive quantile threshold (track a running value quantile, admit top-X% sized to capacity/arrival estimate — i.e., load shedding by criticality). Known art: utility-based load shedding / criticality-aware admission control (e.g. WeChat DAGOR-style, Google criticality shedding) — the search should surface overload-control art and label it. Grade: acceptance + adaptivity honest (no oracle constants tuned on the same seed without a robustness check), full apparatus.