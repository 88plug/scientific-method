Acceptance: cost <=0.55x baseline on seed 30 AND robust (cost reduction holds, even if shallower, on >=3 other seeds). The classical art: Young's formula / Young-Daly optimal checkpoint interval (sqrt(2 * checkpoint_cost * MTBF)) — with pause growing in items, the optimum is much shorter than 60s; solving the tradeoff gives ~10-20s intervals (cost roughly halves). The search MUST surface Young/Daly checkpointing (HPC literature). Grade: acceptance + multi-seed robustness + derivation or adaptive tuning honest + full apparatus (claim, chart, live log citing Young-Daly, honest rung, disclosure).