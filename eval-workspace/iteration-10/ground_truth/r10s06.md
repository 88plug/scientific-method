Acceptance: shared-bytes >=10x fixed-4KB baseline, re-run verified. Fixed blocking after 30 insertions shares almost nothing beyond the prefix (~first insertion point ≈ 1/31 of the file on average); content-defined chunking (rolling hash boundaries — Rabin/buzhash/FastCDC family) re-synchronizes after each insertion and recovers ~95%+ shared bytes → easily 10x. The search MUST surface CDC/rolling-hash dedup (rsync algorithm, LBFS, FastCDC) and label it known. Grade: baseline measured, acceptance met, mechanism cited as known art with deltas, claim chart, honest rung, disclosure.