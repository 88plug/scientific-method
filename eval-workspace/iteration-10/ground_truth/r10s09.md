Acceptance: B==A after protocol, total bytes <=40KB (5%), byte accounting credible, re-run verified. Known-art families the search must surface: Merkle-tree set sync (recursive hash partition descent — passes easily: ~65 differing leaves × log-depth × hash sizes ≈ a few KB), Invertible Bloom Lookup Tables (Eppstein et al. set reconciliation — near-optimal O(d) communication), polynomial set reconciliation (Minsky-Trachtenberg). Grade: acceptance + correct protocol + the search citing Merkle-sync/IBLT-class art as known, claim chart, honest rung, disclosure.