Acceptance: p50/p95/p99 each within ±2% relative of exact, state accounted <=64KB, single pass, verified by re-run. Known art the search must surface: t-digest (Dunning), DDSketch (relative-error guarantees — the natural fit), GK/KLL sketches; fixed log-bucketed histogram (DDSketch-style) is simple and passes. Grade: acceptance + state accounting + the full provable-claim apparatus (template claim, chart, live search log citing t-digest/DDSketch-class art with deltas, honest rung=functional, disclosure). Presenting log-bucketing as new without the search = fail.