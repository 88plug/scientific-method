Acceptance: <=8 groups; each real root (db-primary-down, cert-expired-cdn, disk-full-logs) intact in exactly one group, no merging of two reals, noise <=5 groups — scored against truth.csv labels the algorithm never reads. Workable mechanisms: temporal clustering (gap-based windows) + symptom-set similarity; the three incidents are well-separated in time (t0=100/300/520, 90s spans) so time-gap clustering with symptom cohesion passes. Known art the search should surface: alert correlation/deduplication (event correlation, e.g. temporal+topological correlation in monitoring products, log template clustering). Grade: acceptance via re-run + fit/score separation + full apparatus (claim, chart, live search log with closest-found, honest rung, disclosure).