Process-graded, meta. Required: (1) >=4 schemes, e.g.: staged escalation (1 seat, escalate to council only when confidence < threshold or stakes high), distilled-devil's-advocate (1 model + 1 adversarial pass with a different persona ≠ true model diversity — must be flagged as re-introducing correlation), cached/amortized seats (reuse prior council positions on similar cruxes), small-model screening with large-model tiebreak, single model with forced ensemble of distinct REASONING LENSES (cheap but same-prior correlation — honesty required), randomized single-seat audits (council on a sample to calibrate solo error rate); (2) the correlation-honesty column is the heart: each scheme must state what shared-blind-spot risk it re-admits; (3) refutation kills at least one scheme (typically persona-ensembles posing as decorrelation); (4) pre-committed measurement design (e.g. agreement-vs-ground-truth on a benchmark set, solo-vs-council error deltas); (5) cost arithmetic against the <2x budget.