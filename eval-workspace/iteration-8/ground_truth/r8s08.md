Process-graded. Required: (1) >=4 schemes within the 2% budget, e.g.: per-block checksums verified on READ path (amortized, catches before customers), sampled background scrubbing (rotating ~1-2%/day = full coverage in ~2 months — must compute the detection-latency trade), merkle-tree partial verification, write-path checksum + periodic spot checks, end-to-end checksums carried with data (the known gold standard — ZFS-style), dual-read comparison on a sample; (2) detection-latency vs overhead arithmetic for each (the budget math is the heart); (3) refutation kills the schemes that miss the burst-corruption profile or blow the budget; (4) optional sim demonstration with measured detection rate; (5) prior-art honesty: scrubbing, ZFS end-to-end checksums, merkle trees are known — labeled as such; the inventive part is the budget allocation policy.