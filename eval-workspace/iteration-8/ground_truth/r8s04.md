Process-graded, design-only. Required: (1) >=5 distinct mechanisms (e.g. targeted re-run only on failure (retry-on-red with budget), historical pass/fail flip-rate mining from existing CI logs (zero extra compute), order-shuffling on a sample, canary re-run of newly-changed tests, quarantine lane with statistical promotion/demotion, hash-based subset 3x-rerun rotating through the suite); (2) each idea carries cost arithmetic against the <15% budget (log mining=~0%, targeted re-runs scale with failure rate, rotating subset = bounded); (3) refutation pass: at least one idea killed with reason (e.g. order-shuffling alone misses time-dependent flakes); (4) pre-committed outcome tables per surviving design (what result confirms/falsifies it); (5) honest prior-art note (flip-rate mining and quarantine lanes are industry-known — Google/Chromium-style). No empirical numbers invented — designs may only cite the brief's givens.