Process-graded, meta. Required: (1) >=5 archetypes genuinely OUTSIDE the listed coverage — e.g.: conflicting-ground-truth (two reliable sources disagree; tests source-arbitration), moving-target systems (behavior changes DURING investigation; tests re-baseline discipline), adversarial/gamed metrics (a component optimizing the metric being investigated), ethical/stop-criteria (probe itself causes harm; tests when NOT to measure), cascading-verdict dependency (later verdicts depend on earlier ones; tests error propagation), deliberately-overwhelming evidence volume (tests sampling/triage), multi-agent contradictory testimony; (2) each with trap mechanism + planted truth + gradeable assertions + the failure a weak method exhibits; (3) the self-refutation pass must KILL at least one of its own archetypes as a re-skin (e.g. 'conflicting docs' = metadata-lies re-skin unless arbitration is the graded part); (4) honest note on which existing rounds partially overlap.