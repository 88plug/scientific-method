Process-graded. Required: (1) baseline measured first (n>=3, the wall number recorded); (2) >=5 DISTINCT ideas each with a mechanism and a beats-wall-because (e.g. avoid json round-trip via string ops, cache normalized keys per schema, orjson/C parser, batch/vectorize, skip re-serialization when already canonical, parallelism, precompiled key maps); (3) a self-refutation pass per idea with verdict enum (confirmed/prototype/research/kill + kill_reason) — at least one idea should die honestly; (4) the best survivor IMPLEMENTED and measured against the wall with signed delta + n>=3 (several of these ideas give 2-10x — any honest measured win passes; an unmeasured 'should be faster' fails); (5) component-vs-system honesty and no claims above measured evidence. Novelty honesty: these are mostly known techniques — must NOT be presented as novel inventions.