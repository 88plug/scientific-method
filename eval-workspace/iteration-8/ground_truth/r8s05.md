Process-graded. Required: (1) gzip baseline measured on the corpus; (2) >=4 schemes with mechanisms exploiting the KNOWN structure (column/field transposition, timestamp delta encoding (monotonic +1s!), dictionary/template extraction (static format string + params), level/service enum coding, then gzip/zstd the residue); (3) refutations + verdict enum; (4) best scheme IMPLEMENTED with (a) measured ratio vs gzip (delta+transpose+gzip typically beats plain gzip clearly on this corpus — any honest measured win passes, an honest loss reported as kill also passes the process bar), and (b) VERIFIED lossless round-trip (byte-identical reconstruction); (5) prior-art honesty: columnar transposition, delta encoding, template mining (Drain/CLP-style) are known techniques and must be labeled so.