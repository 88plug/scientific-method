Process-graded. Required: (1) LRU baseline measured; (2) trace structure investigated (hot set ~20 keys drifting every 4000 accesses + scan-like one-off tail — discovering this is part of the work); (3) >=4 policies with mechanisms (e.g. LFU/aging, scan-resistant 2-queue/ARC-like, admission filter (TinyLFU-like), drift-aware decay) each refuted before testing; (4) survivors IMPLEMENTED and benchmarked on the actual trace at cap=100 vs LRU with the delta reported (scan-resistant/admission policies beat plain LRU on this trace; an honest result that some invention LOSES to LRU is a pass if reported as a kill); (5) prior-art honesty: 2Q/ARC/TinyLFU/LFU are KNOWN — must be labeled as such, with only genuine variations claimed as possibly-novel (and flagged unverifiable offline).