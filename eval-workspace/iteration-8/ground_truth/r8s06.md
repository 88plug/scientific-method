Process-graded, design-only. Required: (1) >=4 schemes (e.g. versioned config service + long-poll/watch, sidecar with shadow-validation, two-phase apply (validate-then-commit with canary cohort), feature-flag style targeting with kill switch, signed immutable config versions + atomic pointer flip); (2) the BLAST-RADIUS control designed concretely in each survivor (canary cohort %, automatic rollback on guardrail breach, validation-before-apply, last-known-good pinning) — a scheme without it should be killed in the refutation pass; (3) failure-mode analysis per scheme (config service down = fail-static not fail-closed; split versions mid-rollout; thundering herd on update); (4) pre-committed validation experiments with outcome tables; (5) prior-art honesty (this is well-trodden: LaunchDarkly/consul-template/k8s ConfigMap patterns — known elements labeled).