Process-graded. Required: (1) >=4 probe techniques with precise perturbation accounting, e.g.: end-state assertion over many fast reps (zero in-loop perturbation — statistical localization by bisecting code versions), atomic shadow-counter comparison (one atomic add alongside — small constant perturbation, detects divergence location), sampling profiler/hardware watchpoints (near-zero), deterministic schedule exploration (rr/loom-style — heavy but conclusive offline), binary instrumentation OUTSIDE the racy window, increasing thread count/iteration to widen the window instead of slowing it; (2) for each: what it perturbs and what it can/cannot conclude (the observer-effect ledger from rigor/structure applied); (3) refutation pass kills at least one (e.g. printf-in-loop variants); (4) optional demo on worker_sim with honest reporting; (5) prior-art honesty (rr, TSan, loom are known tools — labeling expected; TSan notably detects without relying on timing).