# FULL-LEVER brief: invent.md pipeline end-to-end — quota ideation (>=6/>=3 lenses),
# refute-by-probe, build, fair gates (tuned baseline/diversity/ablation/holdout),
# provability upgrade where the claim allows, LIVE provenance, claim chart (single
# table), honest rung (witness-artifact rule), disclosure, §8 self-rank.

Problem: de-synchronize cache expiries better than random jitter (sim.py),
including the adversarial all-keys-written-at-once pattern, WITHOUT inflating
mean TTL. Our portfolio has a deterministic low-discrepancy mechanism proven
in retry timing — is a composition into TTL assignment provably better than
random here, and is THAT composition anticipated anywhere? Run the
anticipation + combination tests for the specific composition. If your
mechanism earns no-prior-art-found, say it in corpus-relative grammar.
