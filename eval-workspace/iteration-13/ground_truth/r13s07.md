Acceptance: realized per-key rate deviation provably bounded — a counter-based take-every-100th-per-key (systematic per-key sampling, possibly with golden-ratio/hashed phase to avoid cross-key alignment) gives per-key deviation <=1 event (vs Bernoulli's sqrt(n*p*q)); the bound is exhaustively checkable (for any count n, |samples - n/100| <= 1). Measured comparison vs Bernoulli across >=3 zipf seeds showing the low-volume-key fix. THE PROVENANCE CRUX: systematic sampling is ancient (survey statistics), per-key deterministic sampling exists (consistent/hash sampling in tracing: Dapper-style trace-id sampling is deterministic-by-hash but NOT per-key-rate-bounded; 'rate-limited sampling' in collectors approaches it) — the anticipation/combination tests must be run honestly; the defensible claim is likely the scoped composition (per-key systematic with de-aligned phases, deviation-bound stated) at taught-as-combination or narrow no-prior-art-found. Full lever set; phase-alignment across keys must be addressed (all keys sampling their 1st event = burst at key-join — the de-alignment is where LDS/hash phases earn their keep, and where the fatal-flaw thinking from the retry candidate should transfer).