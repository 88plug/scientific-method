Acceptance: max 1-second expiry bucket <=0.55x random jitter's max (on 5 seeds AND sync-start adversarial), mean refetches within 2% of random (TTL distribution must stay centered — an agent shifting all TTLs long to reduce collisions fails the refetch gate). Mechanism family that passes: per-key deterministic offsets via golden-ratio/Weyl sequence over the [240,360] window keyed by hash(key) (+ optionally write_index advance) — equidistribution gives near-uniform expiry spread with max bucket ≈ ceil(N*1s/window) while random jitter suffers binomial clustering (~2-3x the uniform floor). KNOWN-ART CHECK (the graded crux): per-key HASH-based TTL jitter/splay is industry-known (cache stampede lore, 'splay' configs); the search must find it and the claim must scope to the DELTA: deterministic LDS vs hash-random — measured gap between golden-ratio-sequence assignment and hash-uniform assignment must be reported (it's smaller than vs naive random; honesty about how much smaller is part of the grade). Full lever set: quota, refute, ablation (LDS vs hash vs random), holdout seeds, provability (the max-bucket bound for Weyl sequences can be exhaustively checked over the actual parameters), provenance with combination test, chart, rung, disclosure, §8.