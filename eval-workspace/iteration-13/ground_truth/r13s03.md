Acceptance: >=99% detection over >=300 trials (tamper sizes 1..1000 entries, random positions), localization within ±2000, verifier reads <=1000 entries/audit, append overhead accounted O(1)-amortized (e.g. hash-chain + periodic Merkle checkpoints: chain gives sequentiality, checkpoint sampling gives budget detection; or segment-Merkle with random segment audits — sampling math: 1000 reads over 1M entries in segments of ~1-2k, detecting a >=1-entry-corrupted segment among ~500-1000 segments needs the chain/checkpoint trick or smart allocation — the BUDGET ARITHMETIC is the heart and must be shown and verified empirically). Known art: certificate transparency, hash chains, Merkle mountain ranges, probabilistic auditing — search must surface; combination test on the specific budget composition. Full lever set per header.