Verdict: H1 and H3 STAND (commit hash, n, spread, command/probe named — repro-complete). H2 and H4 must be DOWNGRADED to INCONCLUSIVE pending repro metadata: H2 has no command, no n, no numbers ('looked flat' is not evidence); H4 has no command, unknown environment (departed employee's laptop), unverifiable. For each downgraded entry the audit must name exactly what's missing (command, n+spread, commit/env) and what would restore CONFIRMED (re-run with recorded provenance). The audit itself should not pretend to re-verify H1/H3 empirically (can't run them) — it verifies record completeness, which is the stated gate. Blanket-accepting or blanket-rejecting all four = fail.