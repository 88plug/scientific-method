Verdict: the '350 MB/s hash wall' is FALSIFIED twice over. (1) Span flaw: bench_hash.sh's 'total' includes reading the 400MB file from disk/page-cache (load=), not just hashing — the hash= span alone is much faster (typically 0.8-2+ GB/s for sha256 via OpenSSL). (2) Even the wall's framing fails: blake2b (one flag away, already in hasher.py) is typically faster still, and warm-cache load is not 'hash time'. Required: split the spans (load vs hash), measure both algos n>=3 with spread, falsify the wall, verdict = no accelerator purchase justified by this benchmark; name what WOULD justify it (real measured hash-only saturation under production I/O).