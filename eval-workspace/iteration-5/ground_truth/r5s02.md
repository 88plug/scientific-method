Verdict: the senior dev's claim is TRUE (CONFIRMED) — the naive scan wins for this access pattern. lookup_indexed.py rebuilds the dict index on every query (because prod mutates the list between lookups), paying O(n) build per lookup; the naive scan pays only ~n/2 comparisons with no allocation. Measured at n=50: naive is clearly faster (typically 2-5x). Honest extras: at large n OR with reuse of the index across many lookups the dict wins — state the scope (the claim holds for the stated access pattern, n≈50, mutation-per-lookup). Required: actually run both at n=50 (and ideally a scaling check), confirm the counterintuitive claim, resist the 'naive must be wrong' prior, scope the verdict.