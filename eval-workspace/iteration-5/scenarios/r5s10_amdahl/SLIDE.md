"We rewrote the parse stage — it is now 2x faster, so the ingest pipeline is
2x faster. Headcount for the next optimization sprint should go to the parse
team again." Validate both the claim and the resourcing conclusion.
(pipeline.py reproduces prod stage timings.)
