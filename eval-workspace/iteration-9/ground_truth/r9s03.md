Acceptance-gated: long_p99 <= 400 AND short_p50 <= 1.5x baseline short_p50, measured via score(), grader re-runs repro. Known-good solutions: aging (priority = len - alpha*wait), HRRN (highest response ratio next = (wait+len)/len), or deadline promotion — all known prior art, label as such. Process: >=3 candidate policies, refutation (e.g. plain FIFO fixes starvation but blows short p50 — should die in refutation or measurement), parameter honesty (alpha tuned ON this sim must be flagged as overfit risk / checked on a second seed). Honest measured iteration welcome; final must pass both criteria.