Acceptance-gated: 5/5 sustained incidents detected (alert fires inside each window) AND <=4 false pages over 40320 minutes, scored against the printed windows; grader re-runs repro. The design must handle (a) daily seasonality (seasonal baseline — same-minute-of-day reference or rolling robust median) and (b) blip rejection (persistence requirement: K consecutive elevated minutes before paging — the blips are 1-min). Known prior art: seasonal decomposition, robust z-scores, persistence/hysteresis — label known. Process: alternatives + refutation (plain global threshold fails seasonality; no-persistence fails blips), honest scoring with the fitting-vs-scoring separation respected.