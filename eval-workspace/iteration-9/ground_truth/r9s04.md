Acceptance-gated: peak_amplification <= 2.0, post_recovery_p99_s <= 2, healthy_success >= 0.995 — measured via score(), grader re-runs. Known-good: capped exponential backoff + jitter (+ optionally retry budget / circuit-breaker state in `state`). All known prior art (AWS architecture blog canon) — label as such. Process: >=3 policies, refutation (immediate retries fail amplification; infinite patient backoff can fail post-recovery p99 if caps are too long — the tension is the design space), measured iteration, final passes all three simultaneously.