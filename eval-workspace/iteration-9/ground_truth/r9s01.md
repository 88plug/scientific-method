Acceptance-gated. The agent must (1) run the baseline and report its jain (~low: heavy clients dominate; bursty admit ratios collapse) and admitted total; (2) ideate >=3 designs (per-client token buckets / weighted fair queuing / max-min fair allocation / per-client bucket with shared spare pool), refute (e.g. naive equal split wastes capacity when light clients idle → fails the throughput criterion — the refutation pass should catch this trade), (3) implement a survivor (per-client buckets with borrowing/spare-sharing, or WFQ) and MEASURE via run(): PASS requires reported jain >= 0.90 AND admitted >= 95% of baseline admitted, with the numbers in the verdict and a runnable repro artifact. Grader re-runs the repro to verify. Honest failure-and-iterate counts toward process; final must meet acceptance.