Acceptance-gated: invention's score >= 1.25x baseline score via score(), grader re-runs. The process has a stable hot set (the 40 'hot' values recur ~60% of the time and the 0-400 mid-range another 20%); frequency-based selection (top-50 by COUNT in history) massively beats recency (recency surfaces one-off tail keys). Known prior art: frequency/LFU-style warming — label. Process: >=3 schemes (frequency, recency-weighted frequency, range-density), refutation, the overfitting caveat from the brief addressed (e.g. noting hot-set generalization vs key-identity memorization — frequency works because the hot SET is shared between seeds at the distribution level... an honest check on whether top-frequency history keys recur today is exactly the right probe), measured result.