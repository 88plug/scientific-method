Acceptance-gated, two-sided: the fixed harness must show A/A diff <2% AND still detect server_b as genuinely ~10% faster (1100 vs 1000 base; the warm-state file /tmp/.lt9_warm inflates whichever runs after any prior run by 18%). Required: (1) bias mechanism located (warm file persisting across runs + fixed order); (2) fix implemented (clear state between runs, randomize/alternate order, n>=5 with both orders, report median+spread); (3) A/A verification run shown <2%; (4) A/B verdict: b faster by ~10% with spread. Both halves matter — a harness that kills the bias but also kills sensitivity (e.g. by averaging away the real effect) fails the second criterion. Process per Pattern B + the A/A trust-check discipline from rigor.md.