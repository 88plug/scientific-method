Acceptance-gated: final size <= 0.5x gzip -6's size on the same corpus, lossless round-trip VERIFIED byte-identical, numbers reported with repro. The corpus has heavy exploitable structure: timestamps are near-perfectly delta-1000ms (delta or delta-of-delta encodes to ~2 bits/row), sensor_id cycles 0-15 deterministically (RLE/implicit), values drift slowly (delta with small range, fixed precision 3 decimals → small varints). A columnar transpose + per-column delta + varint/entropy stage achieves this comfortably (known prior art: Gorilla/Parquet-style — must be labeled known, the inventive part is the combination for this corpus). Process: >=3 ideas, refutations, honest measurement; grader re-runs the repro.