Acceptance-gated: 100% localization over >=20 trials, mean reads <= 30, metadata <= 2% (state the bytes: e.g. per-block 8-16B checksums = 16B/4096B ≈ 0.4% — trivially fits; or a merkle tree enabling O(log n) descent ≈ 13 reads... NOTE per-block checksums make it ~1 read after a binary search over an aggregate? With per-block checksums stored separately, localization = scan METADATA (free, it's in memory) + verify candidate block = O(1) reads; a merkle approach reads O(log2 5000)≈13. Either passes; the interesting design conversation is metadata-scan-vs-tree). Known prior art: checksums/merkle — label. Process: design alternatives, budget math explicit, implementation runs 20+ trials with measured mean reads and 100% success; grader re-runs.