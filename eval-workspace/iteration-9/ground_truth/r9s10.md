Acceptance-gated: interactive_p99_ms <= 50 AND batch_done >= 0.8x FIFO baseline's batch_done, via score(); grader re-runs. Known-good designs: separate queues with priority + batch admission only when interactive queue empty AND a batch-share guarantee (e.g. token/credit for batch each N ms), or deficit round robin weighted heavily to interactive. Pure strict priority likely passes batch>=80% here ONLY if interactive load leaves gaps — the agent must MEASURE rather than assume (that's the trap either way). Process: alternatives, refutation, both criteria measured, known-art labeling (priority queues, DRR).