# Problem
Build a detector for metric.csv (gen.py prints the planted incident windows —
use them ONLY for scoring, not for fitting). Acceptance: detect all 5
sustained incidents (alert within their window), with <=4 false pages across
the 4 weeks (1-minute blips must NOT page). The metric has daily seasonality.
INVENT, refute alternatives, implement, score honestly. Numbers + repro.
