# Problem
FIFO head-of-line blocking destroys interactive latency (run sim.py). INVENT
a non-preemptive policy meeting both acceptance criteria. Watch the trap: pure
priority starves batch (fails criterion 2). Pattern B; numbers + repro.
