# Problem
Pick the 50 prewarm keys better than the "most recent" baseline (sim.py;
history.txt is generated on first run). Acceptance: >=1.25x baseline first-10k
hit rate. INVENT selection schemes, refute, measure. Note: today is a fresh
draw from the same process — beware overfitting yesterday's noise.
Numbers + repro.
