# Fair-comparison + ablation brief
Invent a faster query evaluator for this workload (incumbent.py on queries.txt
from gen.py). Claim only what the gates allow (invent.md §3): tune the
incumbent first (e.g. smallest-set-first ordering is an incumbent-level tweak
— does that count as tuning or as your invention? decide and justify), beat
the tuned version, and deliver an ABLATION: which element of your design
carries the win, demonstrated by removing it. Claim chart + provenance +
rung + checksum equality (correctness oracle).
