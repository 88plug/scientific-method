# Provability brief
Rewrite legacy.py's parse_duration (any structure you like — claim: faster
or cleaner) with the claim "behaviorally identical to legacy on ALL string
inputs". Upgrade that equivalence claim as far as the hierarchy allows:
differential property testing (generated strings incl. adversarial), plus
EXHAUSTIVE differential checking over a stated finite scope (e.g. all strings
up to length 5 over a relevant alphabet — state the count). Claim chart with
the rung; the residual (inputs beyond scope) stated honestly. Provenance
brief.
