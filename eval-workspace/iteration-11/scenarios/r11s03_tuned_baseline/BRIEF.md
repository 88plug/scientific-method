# Fair-comparison brief
A colleague's draft claims their rewrite is "4x faster than the incumbent
filter". Their benchmark ran incumbent.py with the shipped config.json.
Your job: produce the FAIR verdict on how much real headroom a rewrite has —
apply invent.md §3 (tuned baseline) before accepting any multiple. Then, if
real headroom remains over the TUNED incumbent, demonstrate it with a
measured implementation. Claim chart + provenance + honest rung.
