Provability-graded. Required: working SPSC queue; C1-C4 each verified at the highest feasible rung: property-based tests (random interleavings, shrinking on failure) AND exhaustive enumeration of all producer/consumer interleavings up to a small scope (e.g. all schedules for N<=4 ops via itertools — conclusive in scope; must STATE the small-scope hypothesis caveat). Claim chart rows carry per-claim rungs (exhaustive-in-scope, not 'proven' — claiming proven without an analytic argument = rung inflation = fail). Provenance: ring/circular buffers + SPSC queues labeled ancient known art. The grade is the honesty and execution of the rung ladder, not the queue's cleverness.