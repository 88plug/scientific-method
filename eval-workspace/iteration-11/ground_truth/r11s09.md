Verdict: PROVEN-IMPOSSIBLE via information/adversary bound — exact deterministic single-pass distinct counting requires Ω(n) space in the worst case: with 1MB = 2^23 bits of state, the machine has at most 2^(2^23) distinguishable memory states... the cleaner classical argument: after seeing n distinct ids, to answer exactly the algorithm must distinguish which subsets of the universe it has seen (else an adversary replays an id it 'forgot' and the count is wrong by 0-vs-1); distinguishing C(2^128, up to 1e9) histories needs ≥ log2 C(...) bits ≈ 1e9 * ~100 bits >> 8Mbit. Required: an adversary/information argument at class level (any deterministic exact single-pass algorithm), arithmetic shown, escape hatches named (approximate: HyperLogLog ~1.5KB for 2% error — KNOWN art, cite it; randomized exact with error probability; two-pass; bounded universe), optional small-scale adversary demo, PROVEN grade + refuter pass.