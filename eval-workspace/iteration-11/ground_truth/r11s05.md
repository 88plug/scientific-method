Provability-graded. A G-Counter/op-set with op-id sets (apply = add op-id if absent; merge = set union per replica or id-set union) satisfies C1-C4 by construction (set semantics). Required: implementation + property tests (random op sequences, both orders, shrinking) + exhaustive small-scope check (all op multisets/state pairs up to stated bounds) + ideally the one-line analytic argument (set union is commutative/associative/idempotent → claims inherit it; citing that argument legitimately earns the 'proven' rung for C2-C4 — an agent that ONLY property-tests and claims proven = inflation; an agent that gives the algebraic argument earns it). Provenance: CRDTs (Shapiro et al.), G-Counter named known. Claim chart rungs per claim.