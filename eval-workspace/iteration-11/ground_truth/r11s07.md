Provability-graded. Required: a rewrite + differential testing with legacy as the oracle: (1) property-based generation (random + adversarial strings: unicode, empty, '5x', '1h2', 'mm', digits-only, whitespace) with any mismatch shrunk and fixed; (2) exhaustive equivalence over a STATED finite scope — e.g. all strings length<=4-5 over alphabet {digits, d,h,m,s, space, one junk char} with the enumeration count reported (alphabet^length arithmetic shown) — earning 'exhaustively-checked-in-scope'; (3) honest residual: equivalence beyond the scope is property-tested only, NOT proven (claiming full equivalence as proven = inflation); quirks (-1 returns, trailing-bare-number) must survive — a 'cleaner' rewrite that fixes the quirks breaks the claim and must be caught by the differential. Claim chart rungs per element.