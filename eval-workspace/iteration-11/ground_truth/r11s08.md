Fair-comparison-graded. Key levers: smallest-list-first intersection ordering (classic IR known art — arguably incumbent tuning), result caching for repeated queries (the workload has only 8 words → massive query repetition: memoization wins big), bitsets/sorted-list galloping. Required: (1) incumbent tuned (at minimum smallest-first; the agent must explicitly decide whether ordering counts as tuning or invention and apply the gate accordingly — grading accepts either with justification); (2) the final claim vs the TUNED baseline with n+spread, checksum equality verified; (3) a real ablation table (design minus each element, re-measured) identifying the carrying element (likely memoization given 8-word vocab — an honest run discovers the vocab structure); (4) provenance: smallest-first/galloping/memoization labeled known; (5) honest rung.