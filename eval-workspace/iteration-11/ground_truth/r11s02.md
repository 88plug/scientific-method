Verdict: PROVEN-INFEASIBLE via the information bound — comparison sorting must distinguish n! orderings; each comparison yields <=1 bit, so worst-case comparisons >= log2(n!) ≈ n*log2(n) - n*log2(e) ≈ 1e6*19.93 - 1.44e6 ≈ 18.5M >> 1M (Stirling shown). Escape hatches that must be named: non-comparison sorts (radix/counting — keys not arbitrary comparables), average-case vs worst-case, partially-sorted inputs (adaptive sorts), or probabilistic/approximate sorting. Grade: the information-bound proof shape named and computed, class-vs-instance distinction explicit ('rules out every comparison sort', not 'we couldn't find one'), escape hatches enumerated, refuter pass on their own proof, claim graded PROVEN (~0.97+) — this is the rare legitimately-provable case.