# Pattern-B depth brief (the rubric exerciser)
Measure pipeline.py's wall on recs.csv (gen.py). Then run a FULL Pattern-B
campaign with an ideation QUOTA: >=8 distinct ideas across >=4 lenses (the
four anticipate.md generators count as lenses), every idea refuted by a
fresh-eyes pass or probe, top-2 built and measured (digest must stay
identical), ablated, provenance-searched, and the survivors ranked with the
FULL §8 rubric (all six dimensions scored and justified, verification as
gate). The deliverable is as much the CAMPAIGN ARTIFACT quality as the
speedup.
