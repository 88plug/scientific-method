# Open invention brief (full pipeline + §8)
Design the bounded-state streaming join meeting sim.py's acceptance (>=99.5%
recall, 0 false joins, <=5MB accounted state vs ~50MB naive). The 5%
stragglers are the hard part — a naive 60s window misses them. Quota/refute/
build/measure; LIVE provenance (stream-join state management has art); chart;
rung; disclosure; §8 ranking.
