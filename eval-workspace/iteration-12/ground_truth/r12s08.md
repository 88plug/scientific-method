Acceptance: recall >=99.5%, false joins =0, state <=5MB accounted, re-run verified. Design space: tiered retention (full recent window + compact long-tail structure for stragglers — e.g. keep 60s of full orders + a compact id->timestamp dict or Bloom-guarded spill for the 600s tail; 500k orders * compact entry must be budgeted), watermark/window join with late-arrival side path. Known art: stream watermarks/allowed-lateness (Flink/Beam), symmetric hash join state eviction — search must surface it. Grade: straggler tier designed with explicit byte budget arithmetic, acceptance, refutations, provenance, chart/rung/disclosure/§8.