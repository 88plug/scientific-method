Improvement-over-own-prior graded. Required: (1) v1 treated as the tuned baseline — run it on the new cases first and document its failure mode (temporal single-linkage merges overlapping incidents into one group; symptom-drift may fragment); (2) generated overlap + drift test cases (fixtures committed, fitting/scoring separation if labels used); (3) v2 (e.g. symptom-cohesion splitting within temporal clusters, drift-tolerant similarity linking) passes new cases AND the original flood C1-C5 unchanged — regression gate explicit; (4) ablation isolating the v2 delta; (5) provenance on the new elements (community detection/correlation clustering art); (6) honest rung + disclosure; the ratchet rule honored (v1's scorecard is the origin baseline).