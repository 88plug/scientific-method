Acceptance: harness drives bump() (unmodified import) to a deterministic lost-update outcome >=99/100 runs (e.g. by instrumenting via sys.settrace/monitoring to force context switches at the read-modify-write boundary, or running bump bodies under a controlled scheduler that interleaves deterministically), localization of the racy pair, and the locked-version control passing. Known art the search must surface: CHESS (Microsoft systematic concurrency testing), loom (Rust), rr chaos mode, PCT (probabilistic concurrency testing), Python sys.setswitchinterval tricks. Grade: determinism demonstrated (>=99/100 identical), control clean, art found+labeled, chart/rung/disclosure/§8.