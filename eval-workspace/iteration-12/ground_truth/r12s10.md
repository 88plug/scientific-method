Portfolio-audit graded. Required per candidate: evidence trail re-examined (artifacts actually opened); one genuinely NEW live query family run and logged; anticipation/combination tests applied with closest-found grammar; rung assigned under the witness-artifact rule — golden-ratio retry legitimately holds `reproduced` (r10s07's independent harness is on disk; verify it), the Bloom rotation correction is `functional` at best (builder-verified, no independent rerun), the detection≠quarantine invariant is a design principle: NOT certifiable on the artifact ladder (no acceptance probe exists) — the honest call is to classify it as a documented design heuristic, not an invention claim, OR design the experiment that would make it testable; §8 scores with verification gating; a defensible external-disclosure recommendation (plausibly: golden-ratio retry = the only publication candidate, with the named path to certified: adversarial peer review + formal anti-clustering verification + remaining venue gaps). Honest demotions score; inflation fails.