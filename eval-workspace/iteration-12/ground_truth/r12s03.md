Acceptance: worst_second_ratio_max <=2.2 across all 30 churn days, re-run verified (random splay ~3-4x via birthday clustering). Strong candidate mechanisms: golden-ratio/LDS assignment by tenant RANK (needs stable ranking under churn — naive rank reshuffles on churn, breaking per-tenant stability: the real design tension), hash-balanced rendezvous into least-loaded-of-2-hashed-slots at assignment time (power-of-two-choices composition — stable per tenant, near-uniform under churn), or jump-consistent-hash-style spreading. Grade: the churn-stability tension recognized and solved (a passing design must hold BOTH stability and spread — measure both), acceptance, refutations incl. at least one design killed by the churn requirement, provenance: hash splay/consistent hashing/two-choices labeled known; if an LDS composition is claimed as no-prior-art-found, the search log must cover scheduled-task-splay art (e.g. 'cron splay', 'jitter scheduled jobs', GCP/SRE guidance) — anticipation test run honestly. §8 ranking with verification gate.