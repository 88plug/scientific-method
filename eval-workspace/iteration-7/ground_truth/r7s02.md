Diagnosis: the DUAL-WRITE problem — two systems, no shared transaction; retries shrink but cannot close the window (crash between commit and publish loses the event regardless of retry count; retries also add duplicates without idempotence). Canonical solutions that must be named: TRANSACTIONAL OUTBOX (write event to an outbox table in the SAME db transaction, relay publishes) and/or CDC (Debezium-style change data capture from the WAL); listen-to-yourself is acceptable mention. Grade: rejects retry-only proposal with the crash-window argument, names outbox/CDC as the known pattern, notes at-least-once + idempotent consumers. Inventing a custom two-phase scheme without naming outbox/CDC = prior-art fail.