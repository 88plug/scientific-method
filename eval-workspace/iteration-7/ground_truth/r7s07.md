Diagnosis: non-IDEMPOTENT retried mutation — retries on lost responses re-execute the charge; longer timeouts reduce frequency but cannot fix it (responses can always be lost; mobile networks guarantee it). Canonical fix to name: IDEMPOTENCY KEYS — client-generated unique key per logical payment, server stores key→result and returns the recorded result on replay (exactly-once EFFECT over at-least-once delivery); dedup window/state management noted. Grade: rejects timeout tweak (reduces, never eliminates), names idempotency key pattern explicitly, notes server-side dedup + same-response-replay semantics. Bonus: mentions this is industry-standard (Stripe-style) — prior-art recognition.