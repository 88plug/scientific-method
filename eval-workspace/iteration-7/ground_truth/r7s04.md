Diagnosis: the N+1 QUERY problem — 1 parent query + 50 per-order item queries + 50 per-item product queries = 101 round trips (~90ms of the 95ms). Canonical fixes to name: eager loading / JOIN or IN-batched queries (SELECT ... WHERE order_id IN (...)), ORM include/prefetch (select_related/prefetch_related, includes, dataloader-style batching). Grade: names N+1 as the known class, quantifies (101 queries), prescribes batching/eager-load, estimates the 2-3 query result. Pure index-tuning or caching answers without naming N+1 = fail.