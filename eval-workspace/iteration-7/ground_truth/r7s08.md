Diagnosis: HOT PARTITION from a monotonically-increasing shard key — all current writes hash to the current-hour shard; adding shards changes nothing (the new current-hour shard is just as hot — 32 shards, same 14k w/s on one). Canonical fixes to name: compound/salted key (device_id + time so writes spread; reads for 'device X last 24h' become single-shard!), key salting/bucketing with scatter-gather where needed, or write-sharding the hot key. The read pattern (per-device) makes device-led compound key the right canonical answer. Grade: rejects add-shards with the monotonic-key argument, names hot-partition/key-design as the known class, proposes device-leading compound or salted key, checks the fix against the stated read pattern.