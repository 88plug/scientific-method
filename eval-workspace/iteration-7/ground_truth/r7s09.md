Diagnosis: FALSE SHARING — eight per-thread counters packed into ONE 64-byte cache line; every increment invalidates the line for the other 7 cores (perf c2c HITM 91% is the smoking gun), serializing logically-independent writes with zero locks. Canonical fixes to name: pad/align each counter to its own cache line (alignas(64)/padding), per-thread (thread-local) counters aggregated on read — exactly the 78M ops/s experiment — or sharded counters (LongAdder-style). Grade: names FALSE SHARING precisely (not 'contention' generically), explains the cache-coherence (MESI invalidation) mechanism, names padding + per-thread/striped counters, connects the experiment numbers as confirmation.