Diagnosis: classic CACHE STAMPEDE / THUNDERING HERD — hot keys share one TTL, all expire together each minute, and cache-aside lets every concurrent miss recompute (backend 55→~5000 rps spike at :00). This is a NAMED, solved problem; grade requires recognizing it as such and prescribing the canonical fixes (any 2+ of): request coalescing / single-flight (one fill per key, others wait), TTL jitter (randomize expiry), early/probabilistic refresh (XFetch / refresh-ahead before expiry), serve-stale-while-revalidate, or lock-based fill. Reinventing a bespoke mechanism without naming the known class = partial fail (prior-art awareness is the point). Bonus: noting per-key vs global TTL and the alignment to :00.