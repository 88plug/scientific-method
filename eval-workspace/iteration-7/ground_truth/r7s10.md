Diagnosis: wall-clock-derived IDs break under clock steps — the known problem class behind SNOWFLAKE-style ID design. Canonical fixes to name (any 2+): monotonic handling in the generator — refuse to issue / wait until last-issued-time is passed (clock-regression guard, as Snowflake does), add a sequence/epoch component that detects regression, use NTP slewing (ntpd/chrony gradual adjustment) instead of stepping in production, or switch to ULID/UUIDv7-style schemes WITH explicit monotonicity guards (naming UUIDv4/random as the always-safe fallback also acceptable). Grade: identifies clock-step as the mechanism, names the snowflake/monotonic-guard pattern or slew-vs-step as known practice, fix guarantees (not merely reduces) uniqueness — e.g. guard + persisted last-timestamp, or non-time-based IDs.