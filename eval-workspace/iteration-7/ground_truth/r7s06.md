Diagnosis: MISSING BACKPRESSURE — an unbounded queue between a 50k/s producer and an 8k/s consumer converts the rate mismatch into unbounded memory growth; doubling memory doubles the time-to-OOM (~40 min) and worsens the crash-loop restart (larger queue to rebuild). Canonical fixes to name: BOUNDED queue + backpressure (block/slow the producer), load shedding with explicit drop policy, or buffering to durable storage (disk/kafka) — plus rate limiting/admission control. The class is flow control; named vocabulary expected (backpressure, bounded buffer, load shedding). Grade: rejects the memory doubling with the arithmetic, names backpressure/bounded-queue, addresses the crash-loop (drain strategy / durable buffer).