Diagnosis: SPLIT-BRAIN from a 2-node cluster with no tiebreaker — lowering the heartbeat timeout makes it WORSE (more spurious promotions on shorter blips). Canonical fixes to name: QUORUM with an odd number of voters (3rd node or lightweight witness/arbiter — majority required to promote), FENCING / STONITH (old primary must be provably stopped or fenced via storage/power before promotion), and lease-based leadership with epochs/fencing tokens so stale leaders' writes are rejected. Grade: rejects the timeout tweak with the partition argument, names quorum+fencing (both ideas), recognizes this as the textbook problem class (consensus/election). Bespoke invention without the known vocabulary = prior-art fail.