Diagnosis: COORDINATED OMISSION (Gil Tene) — a closed-loop tester stops sending while the service stalls, so each 2s stall costs ONE bad sample instead of the ~400 requests that would have arrived at the intended rate during the stall; the percentile is computed over a self-censored sample. Real p99 at any constant arrival rate is catastrophically worse (every stall queues hundreds of intended requests with up-to-2s latency). Canonical fixes to name: open-loop / constant-arrival-rate load generation, latency measured from INTENDED send time (HdrHistogram correction), or wrk2-style pacing. Grade: names coordinated omission (or fully describes the censoring mechanism), explains why users see freezes while the report passes, prescribes open-loop/corrected measurement. Just 'p99.9 shows it' = insufficient (the sample itself is censored).