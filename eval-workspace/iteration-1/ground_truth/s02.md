Verdict: the asserted ~150k rows/s ceiling is FALSE (FALSIFIED). It is derived from the cost of regex validation, but validation is optional: `--no-validate` (documented in --help/docstring) uses split() and is several times faster (typically 3-8x on this box). A real measurement of both modes with n>=3 runs and spread breaks the ceiling. Required: measure baseline first, find the flag (read the code), report median+spread, falsify the derived ceiling.