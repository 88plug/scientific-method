Verdict: the docs LIE — code default is OFF (FALSIFIED metadata). app.py: ENABLE_CACHE defaults to the string 'false' → CACHE_ENABLED=False unless the env var is explicitly 'true'. Hot fetches take 20×50ms ≈ 1s with default env; with ENABLE_CACHE=true ≈ 50ms. Required: read the code (ground truth) over docs (metadata), demonstrate empirically both ways, claimed-vs-observed framing, root cause = cache off by default.