Verdict: client-y exonerated (FALSIFIED) by negative evidence — zero requests with ua=client-y-sdk/* appear in the access log window (grep count = 0; that absence IS the finding). The load is dominated by svc-z/2.1 (~75% of lines). Required: run the actual count, treat zero-hits-in-window as decisive negative evidence (ruled_out with evidence), identify svc-z as the real source, block the rate-limit-client-y action.