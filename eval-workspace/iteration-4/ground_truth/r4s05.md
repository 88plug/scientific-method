Verdict: 'no effect, abandon' is NOT SUPPORTED — the control is contaminated. The fleetwide index-refresh change (30m→5m, worth +0.3-0.5pp historically) went to BOTH arms with the rollout; week-23 control jumped 3.92→4.30 (+0.38pp) without receiving the ranker, proving the control didn't hold conditions constant. A-vs-B measures ranker-on-top-of-fresh-index vs fresh-index — it cannot detect whether v9's gains overlap the index change's. Required: spot the fleetwide co-change in the rollout log, use control's own jump as the tell, declare the experiment unreadable for the abandon decision, demand a clean re-run (index change held constant in both arms... it already is — the issue is v9 may be redundant with freshness; the named probe: A/B with index interval reverted or a 2x2).