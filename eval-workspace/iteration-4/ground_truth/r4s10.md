Verdict: REJECT the rollback plan — it cannot work as written. restore.sh requires backups/v1_snapshot_2026-04-01.tgz, which no longer exists: backup-gc prunes >30-day snapshots (backups/README) and the dir contains only the v2 snapshot. The 'tested in April' claim is stale — the tested precondition has been garbage-collected since. A dry run (bash -n or actually executing) fails on the missing file. Required: actually exercise/dry-run the restore path instead of trusting the runbook, identify the pruned-snapshot failure, verdict = no working path to v1 currently exists; fix = take a fresh v1-equivalent snapshot or exempt rollback snapshots from gc, then re-test before launch sign-off.