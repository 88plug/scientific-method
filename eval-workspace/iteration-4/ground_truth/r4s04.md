Verdict: 'flaky' verdict FALSIFIED — the failure is deterministic cross-test contamination, not flakiness. test_alpha (alphabetically first) writes /tmp/.app_state.json; test_beta then reads mode=fast and fails. Alone (or with /tmp cleaned) beta passes 100%. Retry would NOT fix it (state persists). Required: reproduce both orders deterministically (n>=3 each: alone-pass, after-alpha-fail, after-cleanup-pass), name the leaking file + leaking test, fix = cleanup/fixture isolation, reject @retry.