Verdict: BLOCK the 100% rollout — the median win is real but the tail regressed catastrophically. v1: p50≈100ms, p99≈135-150ms (within SLO). v2: p50≈70ms (-30% ✓ claim true) BUT ~3% of requests land ~900ms → p99 ≈ 900ms+, blowing the p99<500ms SLO. Required: compute the full distribution (p50/p95/p99), confirm the p50 claim honestly, catch the bimodal tail, verdict = SLO guardrail violated, investigate the 3% slow mode before any wider rollout.