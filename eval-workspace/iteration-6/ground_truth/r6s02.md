Verdict: draft REJECTED — this is a minimal cut set of size ≥2 (arguably 3). The outage required cert expiry AND the disabled fallback (would have carried checkout) AND the mis-pointed expiry monitor (would have prevented the expiry). Each is necessary, none sufficient: cert expiry with working fallback = no outage; with working alerting = no expiry. Required: causal-SET verdict naming all jointly-necessary factors with necessity evidence for each, no 'root cause + contributing' demotion (a size-3 cut set forces naming all three), action items for all legs, and ideally common-cause checking. Tiered taxonomy fine: proximate = cert expiry; the set = {expiry, fallback disabled, monitor mis-pointed}.