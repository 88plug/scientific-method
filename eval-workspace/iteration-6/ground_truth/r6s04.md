Verdict: H3 (query-parser) CONFIRMED via least-evidence-against. ACH-style scoring: E2/E5/E7 are inconsistent with H1 (E1, E6 directly inconsistent), H2 (E3 inconsistent; E2 deterministic replay inconsistent with GC nondeterminism), H4 (E4, E7 inconsistent). H3 has ZERO inconsistencies and E2+E5 are highly diagnostic. Note E2 (deterministic repro) is the decisive diagnostic row; E1 alone has low diagnosticity for H2-H4. Required: evidence evaluated against ALL hypotheses simultaneously (matrix or equivalent), explicit inconsistency counts (verdict = fewest-against, not most-support), diagnosticity awareness, and the named confirming probe (fix/escape the parser on that syntax, or feed the failing query to the old parser).