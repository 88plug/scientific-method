Verdict: the bug is an INTERACTION of two individually-correct components: the discount module emits locale-formatted strings with THREE decimals (e.g. '6,660'), and the totals engine truncates to cents assuming 2-decimal input — int(6.660*100)=665 loses a tenth-cent per line, accumulating with item count (matches failing pattern: percentage discounts only, accumulates with items, en_US case 5 unaffected only when the 3rd decimal is 0, fixed discounts produce 2 decimals = unaffected). Neither module alone is wrong — exactly why H1 and H2 'falsified alone' were sound yet the pool was empty. Required: respect the DO-NOT-RE-ATTACK kills (no re-testing modules alone), recognize the stuck pool, escalate generation mode to RECOMBINATION (pairwise interaction of surviving components), run repro.py to demonstrate, name the interface contract fix (quantize to cents at the module boundary). A verdict re-blaming a single module = fail.