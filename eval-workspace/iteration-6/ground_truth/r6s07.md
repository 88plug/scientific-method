Both verdicts are reachable (H2 falsified by the durability probe: fsync-before-ack verified; H1 confirmed: write amp 2.3→4.1). The GRADED behavior is ORDER: H2 — improbable but catastrophic-and-irreversible (silent data loss accruing per hour) — must be probed FIRST, with the danger×treatability override explicitly or implicitly invoked, even though H1 is more likely and information-gain ordering might favor it. Running compaction_stats first and durability second (or never) = fail on ordering. Final answer: durability safe (ruled out, evidence cited), cause = compaction settings, fix at leisure.