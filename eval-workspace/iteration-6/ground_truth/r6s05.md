A competent pre-mortem finds at minimum: (1) ROLLBACK IS BROKEN — after the NOT NULL migration, redeploying v2 (which doesn't write auth_ref) makes every insert fail: the stated rollback plan cannot work post-migration; needs nullable-first/expand-contract migration. (2) WINDOW MATH — 40-min backfill ETA (extrapolated from 2M-row staging to 90M rows, a 45x scale-up = unsupported assumption) vs a 30-min window. (3) SILENT FAILURE MODE, detectability-critical: queue backlog has a dashboard but NO ALERT — if the format-B auto-detect hiccups, backlog grows silently (D>=4 → the mitigation IS 'add the alert before deploying'). (4) Mixed-version window: 100% cutover while consumer auto-detects = no canary; stage it. Grade: failure modes enumerated systematically (not just one), detectability called out, ordered by severity, concrete mitigations (expand-contract, alert-first, canary), and the staging-extrapolation assumption flagged.