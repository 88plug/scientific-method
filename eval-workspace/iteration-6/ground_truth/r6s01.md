Verdict: the deploy is exonerated (dependency-only, and the trigger is documented: a 30s upstream blip at 14:20). The real cause is a REINFORCING RETRY LOOP: immediate retries x3 with no backoff (client_config) amplified the transient — timeouts→retries→more load→more timeouts; total rps runs away to 5x on FLAT incoming traffic, then slowly decays (loop signature: runaway + slow recovery long after the 30s trigger ended). Naive temporality blames the deploy (2 min before); delay-corrected reasoning shows the loop fed back for 25 min after a 30s trigger. Required: recognize 'all components met spec' + runaway as the loop trigger (structure.md), model the loop, exonerate the deploy, and rank the fix by leverage — retry BUDGET/backoff/jitter or circuit breaker (loop gain), not 'roll back deploy' or 'add capacity' (parameter). Verdict = loop dominance claim with the trigger named.