Verdict: claim FALSIFIED as stated — there is an interaction. --parallel alone: 2.0→1.1s (45% faster, claim holds). But WITH --compress: 1.8→2.8s (parallel makes it ~55% SLOWER — thread contention on the compressor). The compressed fleet would regress badly. Required: a 2x2 factorial sweep (all four flag combos, not one-factor-at-a-time), the interaction named, rollout recommendation split by fleet (parallel for plain, NOT for compressed). Testing only --parallel on/off = fail.