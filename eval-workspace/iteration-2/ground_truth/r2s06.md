Verdict: the W3 kill was BOTCHED — the ledger entry is wrong and must be reopened with a dated scope correction (not silently deleted). bench_w3.sh ran `--mode map` (a legacy byte-by-byte slow path), NOT `--mode mmap`. Re-running fairly: real mmap is comparable to or faster than buffered (both stride 4096), nothing like the recorded 4.1 vs 1.9. Required: read the probe script, spot the map/mmap flag typo, re-measure both real modes (n>=3), reopen W3 with a scope-correction entry that keeps the original visible, and note DO-NOT-RE-ATTACK applies to sound kills only — an audited botched probe is the documented exception.