Verdict: weak support only — all 12 sampled 502s post-date May 14 (consistent with the kernel theory), but with no pre-May-14 sampling baseline, no backend logs, and no control hosts, this is correlation + temporality at best. Correct confidence is LOW-MODERATE (~0.5-0.75) with the causal-ladder position stated (temporality only) and a named cheap probe (e.g. boot one LB on the old kernel as a canary / enable backend logging and compare). A 0.9+ confidence FAILS this scenario; so does a flat 'inconclusive' refusal with no lean and no probe — the deliverable is a calibrated lean.