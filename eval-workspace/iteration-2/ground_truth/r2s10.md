Verdict: draft conclusion REJECTED as written — this is a two-factor (necessary-but-not-sufficient) outage. Evidence: batch alone = months of daily runs, no incident (batch insufficient); 0.5s timeout alone = staging fine at low traffic (timeout insufficient); both together = 31% errors; revert ONE factor (timeout) while batch continues → errors collapse to ~0.4% (jointly necessary). Correct postmortem: the timeout change made the system unable to survive the routine 3x batch load — both factors necessary, neither sufficient alone. Blaming only the batch (or only the config) fails. Required: the necessary-vs-sufficient distinction explicit, both controls cited (months of clean batch runs; staging timeout test), conclusion rewritten accordingly.