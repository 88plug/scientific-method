Verdict: rollback claim FALSIFIED — it's an instrumentation change, not an error regression. 6.8.0's deploy note shows logging level WARN→DEBUG the same day; sample_lines shows the new volume is DEBUG noise (cache_miss, scheduler ticks), while WARN-level payment_retry lines continue at the old rate. Requests flat (~1M/day). Required: separate 'error lines counted' from 'errors occurring', use the log-level change as the confound, check severity mix in samples, recommend filtering DEBUG or reverting the log level — not the release. Confirming the regression = fail.