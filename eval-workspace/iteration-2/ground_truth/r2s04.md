Verdict: BLOCK the PR despite the real speed win. new_impl is genuinely faster (vectorized parse, typically 30-45%) BUT slurps the file and builds a list+dict — peak RSS goes from a few MB (streaming) to several hundred MB (often >512MB cgroup cap → OOM-kill risk on the fleet). The PR's own context (512MB cap) makes memory a guardrail metric. Required: measure BOTH speed (confirm the claim, signed delta) and peak memory (the regression, signed delta), verdict = speed win real but guardrail regressed, do not deploy as-is on capped workers. Missing the memory check = fail.